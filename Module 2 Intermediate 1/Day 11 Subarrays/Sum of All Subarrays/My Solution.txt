public class Solution 
{
    public long subarraySum(int[] arr)
    {
        long ans = 0;
        int N = arr.length;

        for(int i = 0; i < N; i++)
        {
            ans = ans + ((long)arr[i] * (i + 1) * (N - i));
        }

        return ans;
    }
}